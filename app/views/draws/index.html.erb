<h1>Listing draws</h1>

<table>
  <tr>
    <th>Image</th>
    <th>User</th>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @draws.each do |draw| %>
  <tr>
    <td><%= image_tag draw.avatar.url(:medium) %><td>
      <td><%= draw.user_id %></td>
      <td><%= draw.title %></td>
    <td><%= draw.description %></td>
  
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Draw', new_draw_path %>

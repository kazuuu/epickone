<%= form_for(@drawship) do |f| %>
  <% if @drawship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drawship.errors.count, "error") %> prohibited this drawship from being saved:</h2>

      <ul>
      <% @drawship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :draw_id %><br />
    <%= f.number_field :draw_id %>
  </div>
  <div class="field">
    <%= f.label :picked_number %><br />
    <%= f.number_field :picked_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

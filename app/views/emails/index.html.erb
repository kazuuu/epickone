<h1>Emails</h1>

<%= @user.email %> (E-Mail Principal)
<br>
<% @user.emails.each do |e| %> 
	<%= e.email %> <%= link_to "Definir como Pricipal", "" %> <%= link_to 'Remover', e, method: :delete, data: { confirm: 'Tem certeza?' } %>
	<br>
<% end %>

<br>
<%= link_to "Adicionar novo Email", "" %> 






<div class="row">
  <div class="col-md-12">  
    <table class="table table-striped">
      <thead>
        <tr>
          <th class="col-md-2">
            Data do Resultado
          </th>
          <th class="scol-md-7">
            Evento
          </th>
          <th class="col-md-1">
            Número
          </th>
        </tr>
      </thead>

      <tbody>
<% if @tickets_old.count <= 0  %>
				<tr>
					<td colspan='2'>
						Nenhum ticket.
					</td>
					<td>
					</td>
				</tr>
<% else %>
	<% @tickets_old.each do |item| %>
        <tr>
          <td>
              <%= l item.end_date.to_date, :format => :simple if !item.end_date.nil? %> 
          </td>
          <td>
            <%= link_to item.event.title, item.event %>
          </td>
          <td>
            <%= item.picked_number_formatted if !item.picked_number.nil? %>
          </td>
        </tr>
	<% end %>
<% end %>
      </tbody>
    </table>
  </div> 
</div> <!-- class row -->

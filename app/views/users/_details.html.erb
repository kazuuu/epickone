<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= image_tag @user.avatar.url(:thumb) %>
        <%= @user.first_name %> <br>
        <%= current_user.friends_count %><br><br>
      </h1>
    </section>
  </aside>
</div>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<% unless @user.avatar_url.blank? %>
	<li>Avatar: <%= image_tag @user.avatar_url %></li>
<% end %>
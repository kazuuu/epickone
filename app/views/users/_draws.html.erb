<%
  @tickets_events = current_user.tickets.find(:all, :joins => :cart, :conditions => "carts.purchased_at Is Null", :select => "event_id", :group => "event_id")
 
 
  @tickets_events.each do |event_cart| 
%>

    <b><%= event_cart.event.title%> </b><br>

<%    
    @tickets = current_user.tickets.find(:all, :joins => :cart, :conditions => "carts.purchased_at Is Null and event_id=" + event_cart.event.id.to_s)
    
    @tickets.each do |item|
      %>
      <%=item.picked_number%> <br>
    
    
      <% end %> 
<% end %>
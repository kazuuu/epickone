<h1>Listing Products</h1>

<table>
  <tr>
    <th>Image</th>
    <th>User</th>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= link_to(image_tag(product.avatar.url(:thumb)), store_product_path(product)) %><td>
      <td><%= product.title %></td>
    <td><%= product.description %></td>
  
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_store_path %>
